#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
	int n; cin>>n;
	ll ans = 0;
	ll five = 5;
	while (five <= n) {
		ans += (n/five);
		five *= 5;
	}
	cout << ans;
	return 0;
}

